✅ THE COMPLETE REPLIT PROMPT (Copy & Paste This)

TITLE:
Build “SFD Engine — Mobile Explorer v1.5”

INSTRUCTIONS:
Create a mobile-optimized, touch-native version of the Structural Field Dynamics (SFD) engine. This app should run fully inside a single-page web app (HTML/CSS/JS) and be optimized for mobile screens (iOS + Android). DO NOT replicate the desktop UI. This is intentionally simplified, touch-oriented, and visually dramatic.

⸻

APP REQUIREMENTS — CORE

1. Dynamic Field Renderer

Build a GLSL/WebGL or Canvas-based 2D field simulation that renders a scalar field evolving iteratively over time. The field must:
	•	update every animation frame
	•	support parameter-driven deformation
	•	support multiple operator regimes
	•	blur/interpolate slightly for smoother visuals

Field resolution: ~256×256 for mobile performance.

⸻

2. Touch-Reactive Field Deformation

When a user touches or drags on the field:
	•	inject a localized perturbation
	•	create a temporary curvature spike (κ)
	•	visually deform the field (brightness or displacement)
	•	relax back dynamically over ~400ms

This MUST feel alive and responsive.

⸻

3. Swipe Regime Selector

A horizontal swipe gesture changes the operator regime:

Regimes (in order):
	•	E = Equilibrium
	•	K = Kappa-Shear
	•	T = Tension
	•	F = Fractal
	•	C = Collapse

On swipe:
	•	update regime
	•	animate subtle field morph
	•	show a 0.5s overlay text (e.g. “Fractal Mode”)

⸻

4. Floating Diagnostics Capsule (Minimal)

Add a small translucent floating box in top-left corner showing:

Stable / Unstable
κ: 0.013
ε: -0.028
τ: 0.004

These values should be sampled from the current field state (or approximated if no full solver is implemented).

Update every 1 second.

⸻

5. Parameter Radial Wheel

At the bottom toolbar, add a button “Params”.

On tap:
	•	slide up a radial control wheel
	•	show adjustable parameters: κ, τ, γ, α, ρ
	•	selecting one parameter opens a curved slider
	•	adjusting parameters immediately affects the field

Make this visually unique — not standard sliders.

⸻

6. Instability Threshold Flash

If any parameter combination pushes the field into instability:
	•	flash faint red on the screen edges
	•	shake the diagnostics capsule slightly
	•	destabilize the field texture
	•	after 1 second, allow user interaction again

This is purely aesthetic, not destructive.

⸻

7. Viral Capture Button

Add a “Capture” button (camera icon).

When tapped:
	•	export a high-res PNG of the current field
	•	include:
	•	field image
	•	small corner diagnostic readout
	•	regime icon
	•	SFD watermark (“SFD Engine — Structural Field Explorer”)
	•	open share menu (navigator.share when available)

This button is crucial for virality.

⸻

8. Bottom Navigation Bar (Minimal Icons)

Persistent bottom bar with:
	•	▶️ Run / Pause
	•	⚡ Regimes
	•	🎨 Colors
	•	⚙️ Params
	•	🔗 Share

Icons must be circular, clean, and minimal.

⸻

9. Color Maps

Implement 3 color maps:
	•	Viridis
	•	Inferno
	•	Cosmic (custom purple-blue)

Tap the Colors icon → simple 3-button selector with animated transitions.

⸻

10. Tilt-Based Parallax Depth (Optional But Add It)

Use device orientation events:
	•	tilt phone → slight parallax shift in the field
	•	extremely subtle (1–3 pixels)

This creates a “depth cavity” illusion.

⸻

11. Architecture / File Requirements

Generate:

index.html
styles.css
app.js
field.js
regimes.js
params.js
colorMaps.js
capture.js
touch.js
diagnostics.js

Keep all logic modular and readable.

Use:
	•	mobile-first responsive CSS
	•	requestAnimationFrame loop
	•	no external libraries unless necessary

⸻

12. Visual Style Requirements

Dark theme only.

Use:
	•	rounded edges
	•	subtle glows
	•	frosted translucent UI panels
	•	smooth transitions
	•	slightly futuristic aesthetic

NO clutter.

Everything must feel calm, scientific, elegant.

⸻

DELIVERABLE:

A complete, runnable mobile web app meeting all requirements above.